name: Build and Push Custom Keycloak Image

on:
  push:
    branches: [main]
    paths:
      - "docker/keycloak/**"
  pull_request:
    branches: [main]
    paths:
      - "docker/keycloak/**"
  workflow_dispatch:
    inputs:
      keycloak_version:
        description: "Keycloak version to build"
        required: false
        default: "26.3.5"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/bitnami-keycloak

jobs:
  build-keycloak:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=sha,prefix={{branch}}-
            type=raw,value=${{ github.event.inputs.keycloak_version || '26.3.5' }}
            type=raw,value=${{ github.event.inputs.keycloak_version || '26.3.5' }}-minimal
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=minimal,enable={{is_default_branch}}

      - name: Build and push Keycloak Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./docker/keycloak
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            KEYCLOAK_VERSION=${{ github.event.inputs.keycloak_version || '26.3.5' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Output image details
        run: |
          echo "Built and pushed image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          echo "Tags: ${{ steps.meta.outputs.tags }}"
